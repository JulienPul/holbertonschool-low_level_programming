#include <stdio.h>

int main(void)
/**main:description of th project*/

{
	{
	char ch;

for (ch = 'a'; ch <= 'z'; ch++) /** loop a a z */
	putchar(ch);
	}
	putchar('\n'); /**retour ligne*/

	return (0);
}

#include <stdio.h>
#include "main.h"
/**
 * swap -  swaps the values of two integers.
 * Description: jijhfg
 *t: variable temporire
 */
void swap_int(int *a, int *b)
{
	int t;

	t = *a;
		*a = *b;
		*b = t;
}

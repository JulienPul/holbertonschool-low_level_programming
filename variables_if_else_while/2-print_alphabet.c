#include <stdio.h>

int main(void)
/**
 * main - Entry point
 *
 * Description: a a z
 */
{
	{
	char ch;

for (ch = 'a'; ch <= 'z'; ch++) /** loop a a z */
	putchar(ch);
	}
	putchar('\n'); /**geeghhgerejr*/

	return (0);
}
